# Test fixtures

Store only synthetic, non-sensitive data and intentionally vulnerable local lab
fixtures here. Include fixed counterparts so tests can check false positives.
Never add real customer responses, credentials, session tokens, or personal data.
