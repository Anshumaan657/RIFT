# Vulnerable lab profile

This profile intentionally omits authentication and ownership enforcement on the
private-record endpoint. It exists only to prove the two V1 access-control checks.
Run it through the isolated Compose configuration; never deploy it publicly.
